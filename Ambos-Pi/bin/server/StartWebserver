#!/bin/bash
mate-terminal --command "/usr/bin/python /home/ambos/AMBOS-PbL/bin/server/webserver.py"

# check if AMBOS webserver is already running, otherwise start it
#/usr/bin/python /home/ambos/AMBOS-PbL/bin/server/check_webserver.py
#ret=$?
#echo $ret
#if [ $ret -ne 0 ]; then
#    echo "Webserver is not running. Starting Webserver..."
#    mate-terminal --command "/usr/bin/python ~/AMBOS-PbL/bin/server/webserver.py"
#fi

